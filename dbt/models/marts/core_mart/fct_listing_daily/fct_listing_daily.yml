version: 2

models:
  - name: fct_listing_daily
    description: >
      One row per listing per date — the atomic fact table for revenue and
      pricing analysis.
    config:
      tags: ['1x_day']
    columns:
      - name: calendar_id
        description: >
          {{ doc('calendar_id') }}

          Primary key here.
        data_tests:
          - unique

      - name: listing_id
        description: >
          {{ doc('listing_id') }}

          Foreign key to dim_listings.
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_listings')
                field: listing_id

      - name: calendar_date
        description: "{{ doc('calendar_date') }}"
        data_tests:
          - not_null

      - name: month_start_date
        description: >
          First day of the month containing `calendar_date`, precomputed because
          every revenue-by-month query needs it and date_trunc in a group by is
          easy to get subtly wrong.
        data_tests:
          - not_null

      - name: is_available
        description: "{{ doc('is_available') }}"
        data_tests:
          - not_null

      - name: reservation_id
        description: "{{ doc('reservation_id') }}"

      - name: price
        description: "{{ doc('nightly_price') }}"
        data_tests:
          - not_null

      - name: revenue
        description: "{{ doc('revenue') }}"

      - name: minimum_nights
        description: "{{ doc('minimum_nights') }}"

      - name: maximum_nights
        description: "{{ doc('maximum_nights') }}"

      - name: is_window_start
        description: "{{ doc('is_window_start') }}"

      - name: availability_window_seq
        description: "{{ doc('availability_window_seq') }}"

      - name: neighborhood
        description: "{{ doc('neighborhood') }}"

      - name: property_type
        description: "{{ doc('property_type') }}"

      - name: room_type
        description: "{{ doc('room_type') }}"

      - name: accommodates
        description: "{{ doc('accommodates') }}"

      - name: bedrooms
        description: "{{ doc('bedrooms') }}"

      - name: beds
        description: "{{ doc('beds') }}"

      - name: host_id
        description: "{{ doc('host_id') }}"

      - name: has_air_conditioning
        description: "{{ doc('has_air_conditioning') }}"
        data_tests:
          - not_null

      - name: has_lockbox
        description: "{{ doc('has_lockbox') }}"
        data_tests:
          - not_null

      - name: has_first_aid_kit
        description: "{{ doc('has_first_aid_kit') }}"
        data_tests:
          - not_null

      - name: amenity_count
        description: "{{ doc('amenity_count') }}"

      - name: is_orphan_listing
        description: "{{ doc('is_orphan_listing') }}"
    data_tests:
      - dbt_utils.equal_rowcount:
          arguments:
            compare_model: ref('int_listing_daily')
